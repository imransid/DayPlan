/**
 * Dev sign-in defaults. Keep in sync with backend/prisma/seed.ts (run `npm run db:seed` in backend).
 */
export const DEMO_EMAIL = 'demo@dayplan.local';
export const DEMO_PASSWORD = 'demo12345';
